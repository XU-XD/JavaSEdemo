1、随意定义一个变量i = 3287;获取该数字中每位上的数字
		最终输出：个位是7， 十位是8，百位是2， 千位是3

2、给定i和j，int i = 10, j = 20; int z = (i++) *2+ (++j) *2+ (++i)%2 + (j++)/2;
		不要写代码，计算i、j、z的值

3、定义一个int变量，使用三元运算符，输出这个变量是否可以被7整除
		例如，定义变量int a = 10;
		最终输出：变量值为10，该变量不能被7整除

4、执行下列程序段后，m,x,y的值分别是？
	
	int x = 2,y = 4;
	boolean m;
	m = ++x > y--;

	A. true,2,4
	B. false,3,3
	C. true,3,3
	D.false,2,4
