1、笔记中标红部分：抽查提问

2、  (基本类型的运算)一家商场在举行打折促销，所有商品都进行8 折优惠。
       比如商品的原价为：int price = 100;
       计算打折后的价格：int realPrice = price * 0.8；
	问：上面那段代码是否正确？为什么？

3、定义如下两个变量，如：
     int a = 10;
     int b = 20;
     使用字符串拼接在控制台打印指定结果（打印语句中不能出现10和20）：
     打印结果：a = 20,b = 20；

4、定义如下两个变量，如：
     int x = 10;
     int y = 20;
     使用字符串拼接，在控制台打印指定结果（打印语句中不能出现10和20）：
     打印结果： x是10，y是20，x+y是30；